# TicTacToe-with-Bitwise-Operations
TicTacToe where the datastructure is a list of two binary numbers, one for each player. Bitwise operations are done to evaluate win conditions, insert symbols to the board, and check whether a location on the board is already occupied.
